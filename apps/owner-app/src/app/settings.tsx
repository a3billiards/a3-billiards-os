import { View, Text } from 'react-native';

export default function Screen() {
  return (
    <View style={{ flex:1, justifyContent:'center', alignItems:'center' }}>
      <Text>Screen — coming in Phase 9</Text>
    </View>
  );
}
